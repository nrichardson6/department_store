<h1><%= @item.id ? 'Edit Item' : 'Create Item' %></h1>
<hr />
<%= form_for [@department, @item] do |f| %>
  <%= f.label :name, :price %>
  <%= f.text_field :name, required: true, :price, required: true,  autofocus: true %>
  <%= f.submit %>
<% end %>