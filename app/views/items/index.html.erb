<h1>Click an Item To View Details</h1>
<%= link_to 'Create a new item', new_department_item_path(@item) %>
<ul>
  <% @items.each do |item| %>
    <li><%= link_to item.name, [@department, item] %></li>
  <% end %>
</ul>